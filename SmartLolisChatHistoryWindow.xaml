<Window x:Class="VPet.Plugin.SmartLolis.SmartLolisChatHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Chat History"
        Width="640"
        Height="580"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip"
        Background="#F5F7FA">
    <Window.Resources>
        <SolidColorBrush x:Key="BorderBrush" Color="#C9D7E2"/>
        <SolidColorBrush x:Key="BorderBrightBrush" Color="#7FC7F0"/>
        <SolidColorBrush x:Key="HeadBrush" Color="#2A6D96"/>
        <SolidColorBrush x:Key="TextBrush" Color="#324B5C"/>
        <SolidColorBrush x:Key="MutedBrush" Color="#768A98"/>
        <SolidColorBrush x:Key="AccentAzureBrush" Color="#45ACE3"/>
        <SolidColorBrush x:Key="GlassPanelBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="InputBrush" Color="#FFFFFF"/>

        <Style x:Key="CalendarButtonBaseStyle" TargetType="Button">
            <Setter Property="Width" Value="131" />
            <Setter Property="Height" Value="38" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Foreground" Value="#2A5975" />
            <Setter Property="FontFamily" Value="Bahnschrift" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                SnapsToDevicePixels="True">
                            <Grid>
                                <Border Height="2"
                                        VerticalAlignment="Top"
                                        Background="{TemplateBinding BorderBrush}"
                                        Opacity="0.9" />
                                <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"
                                                  Margin="10,0"
                                                  RecognizesAccessKey="True" />
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.92" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Root" Property="Opacity" Value="0.82" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource CalendarButtonBaseStyle}">
            <Setter Property="Background" Value="#D8F0FF" />
            <Setter Property="BorderBrush" Value="#59B7EA" />
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource CalendarButtonBaseStyle}">
            <Setter Property="Background" Value="#F5FCFF" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
        </Style>

        <Style x:Key="PurpleButtonStyle" TargetType="Button" BasedOn="{StaticResource CalendarButtonBaseStyle}">
            <Setter Property="Background" Value="#ECE3FF" />
            <Setter Property="BorderBrush" Value="#9C88E8" />
        </Style>

        <Style x:Key="SmallSecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource CalendarButtonBaseStyle}">
            <Setter Property="Width" Value="Auto" />
            <Setter Property="MinWidth" Value="60" />
            <Setter Property="Height" Value="26" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Padding" Value="8,2" />
            <Setter Property="Margin" Value="0,0,6,0" />
            <Setter Property="Background" Value="#F5FCFF" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
        </Style>

        <Style x:Key="HistoryTextBoxStyle" TargetType="TextBox">
            <Setter Property="FontFamily" Value="Consolas" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="Background" Value="{StaticResource InputBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="8,6" />
            <Setter Property="AcceptsReturn" Value="True" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="VerticalContentAlignment" Value="Top" />
            <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="TextBoxBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Padding="{TemplateBinding Padding}"
                                          VerticalScrollBarVisibility="{TemplateBinding VerticalScrollBarVisibility}"
                                          HorizontalScrollBarVisibility="Auto" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="TextBoxBorder" Property="BorderBrush" Value="{StaticResource BorderBrightBrush}" />
                            </Trigger>
                            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                <Setter TargetName="TextBoxBorder" Property="BorderBrush" Value="{StaticResource AccentAzureBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="#F5F7FA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" MinHeight="80" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Margin="12,12,12,0" Padding="14,10" Background="{StaticResource GlassPanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock x:Name="txtTitle" Text="CHAT HISTORY" Foreground="{StaticResource HeadBrush}" FontFamily="Bahnschrift SemiBold" FontSize="22" />
                    <TextBlock x:Name="txtSubtitle" Margin="0,4,0,0" Text="View and edit conversation messages." Foreground="{StaticResource MutedBrush}" FontSize="12" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="txtCount" Text="0 messages" Foreground="{StaticResource HeadBrush}" FontFamily="Bahnschrift SemiBold" FontSize="14" VerticalAlignment="Center" Margin="0,0,12,0" />
                    <Button x:Name="btnRefresh" Content="Refresh" Style="{StaticResource SecondaryButtonStyle}" Click="BtnRefresh_Click" />
                </StackPanel>
            </Grid>
        </Border>

        <TextBox x:Name="txtHistory" Grid.Row="1" Margin="12,8,12,0" Style="{StaticResource HistoryTextBoxStyle}" />

        <GridSplitter Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Stretch" Height="6" Margin="12,0,12,0" Cursor="SizeNS" Background="Transparent" />

        <Border Grid.Row="3" Margin="12,2,12,0" Padding="12,10" Background="{StaticResource GlassPanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="6">
            <DockPanel>
                <DockPanel DockPanel.Dock="Top" Margin="0,0,0,6">
                    <TextBlock x:Name="txtCompressedLabel" Text="COMPRESSED MEMORY" Foreground="{StaticResource HeadBrush}" FontFamily="Bahnschrift SemiBold" FontSize="14" DockPanel.Dock="Left" VerticalAlignment="Center" />
                    <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <Button x:Name="btnExport" Content="Export" Style="{StaticResource SmallSecondaryButtonStyle}" Click="BtnExport_Click" />
                        <Button x:Name="btnImport" Content="Import" Style="{StaticResource SmallSecondaryButtonStyle}" Click="BtnImport_Click" Margin="0" />
                    </StackPanel>
                </DockPanel>
                <TextBlock x:Name="txtCompressedHint" DockPanel.Dock="Top" Text="Summary of older conversations. You can edit this manually or paste an AI-processed summary." Foreground="{StaticResource MutedBrush}" FontSize="11" Margin="0,0,0,6" />
                <TextBox x:Name="txtCompressed" Style="{StaticResource HistoryTextBoxStyle}" />
            </DockPanel>
        </Border>

        <Border Grid.Row="4" Margin="12,8,12,12" Padding="10" Background="{StaticResource GlassPanelBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="6">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="btnCompress" Content="Compress Memory" Style="{StaticResource PurpleButtonStyle}" Click="BtnCompress_Click" />
                <Button x:Name="btnSave" Content="Save" Style="{StaticResource SecondaryButtonStyle}" Click="BtnSave_Click" />
                <Button x:Name="btnCancel" Content="Close" Style="{StaticResource SecondaryButtonStyle}" Margin="0" Click="BtnCancel_Click" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
